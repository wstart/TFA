.build/
.swiftpm/
DerivedData/
*.xcodeproj
*.xcworkspace
.DS_Store
docs/.tmp/
/tmp-research/
*.app
# Editor swap / backup files
*.swp
*.swo
*~
# Local-only tests — kept out of the public repo by request. Package.swift adds the test
# targets ONLY when Tests/ exists (FileManager check), so a fresh clone still builds fine.
/Tests/
.gstack/
